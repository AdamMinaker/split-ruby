<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="alert alert-primary" id="notice"><%= notice %></p>
    <% end %>

    <%= render @session %>

    <div class="inline-block d-flex ml-2 mt-1">
      <%= link_to "Back", "/exercises/#{@session.exercise_id}", class: "btn btn-sm btn-outline-primary me-1" %>
      <%= link_to 'Edit', edit_session_path(@session), class: "btn btn-sm btn-outline-primary me-1" %>
      <%= button_to 'Delete', session_path(@session), method: :delete, class: "btn btn-sm btn-outline-primary" %>
    </div>
  </div>
</div>
